# 构建基于 HTMX 与 Markdown 的个人 AI 知识库与 Agent

## 1. 核心理念：为什么选择 Markdown？
在 AI 时代，`.md` 文件凭借其独特的优势，成为了连接人类与大模型之间的“通用语”：
* **极致的 Token 经济性**：摒弃冗余的 HTML 标签，大幅减少 Token 消耗，扩充上下文窗口。
* **大模型的“母语”**：AI 训练语料库中包含海量 GitHub、Stack Overflow 的 Markdown 数据，模型天生擅长理解和输出此格式。
* **双向可读与高容错**：即使不渲染，人类阅读也毫无障碍；即使 AI 漏写闭合符号，也能平滑降级，不会导致整个结构崩溃报错。

## 2. 技术选型：极简前端与 HTMX 的绝佳配合
摒弃复杂的 React/Vue 打包生态，针对“个人知识库”这种以阅读为主的场景，采用 **HTMX + 服务端渲染** 是极度优雅的方案：
* **工作流**：后端将 Markdown 解析为 HTML 片段 -> HTMX 通过网络直接将 HTML 注入前端 DOM，无需页面刷新。
* **极致轻量**：前端只需引入一个 14KB 的 HTMX 脚本，无构建工具，零 JS 状态管理。

## 3. 基础架构：Hono + HTMX
使用 JS/TS 生态中的轻量级框架 Hono 配合 HTMX，实现“本地优先”的数字笔记本：
* **技术栈**：Bun/Deno + Hono + HTMX + `marked` (Markdown 解析库)。
* **数据存储**：完全基于文件系统，笔记以 `.md` 格式直接存放在本地 `data/` 目录中，数据拥有绝对掌控权。
* **核心功能 SOP**：
  1. 与 AI 对话解决问题。
  2. 使用标准化 Prompt 让 AI 提炼对话（背景 + 原理 + 方案）。
  3. 后端接收并保存为 `.md` 文件，HTMX 动态刷新侧边栏目录，点击即可丝滑阅读。

## 4. 进阶路线：进化为“专属个人 Agent” (RAG 架构)
将静态笔记系统升级为具备“思考和检索”能力的专属 AI 助手。

### 技术栈扩展
* **向量数据库 (Vector DB)**：引入轻量级、无服务器的方案（如 LanceDB 或 Orama），与 Hono 共存，无需独立部署。
* **大模型驱动**：接入本地 Ollama（注重隐私）或 OpenAI API。

### Agent 核心工作流
1. **知识入库 (Embedding)**：每次保存 `.md` 时，后台自动将文章切片（Chunking），调用大模型转换为多维向量，存入本地数据库。
2. **语义检索**：用户在知识库前端提问（通过 HTMX 提交）。
3. **上下文组装**：系统从向量数据库中检索出最相关的 3-5 篇历史笔记片段。
4. **大模型生成**：将检索到的笔记与用户问题拼接成特定 Prompt，要求 AI **严格基于历史笔记**作答，最终通过 HTMX 将回答返回给前端。

### 高阶拓展 (Function Calling)
* **自动整理**：Agent 后台自动阅读新笔记，生成 YAML Frontmatter（标签、分类）并归档。
* **自我进化**：当提问超出当前知识库范围时，Agent 自动调用搜索引擎查找答案，总结为新的 `.md` 文件入库，实现知识库的自我生长。