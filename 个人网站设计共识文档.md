# mylog — 基于 Cloudflare + Hono + HTMX 的 Markdown 智能网盘与 AI 知识库
## 核心设计共识与技术规范文档（终极定稿）

> 本文档用于指导系统架构与代码实现。本项目遵循**「极简代码、极致性能、移动端优先、纯粹工程质感」**原则，专注于为 Markdown 提供专属的网盘存储、权限管理与未来语义智能。

---

## 一、产品定位与演进三阶段

传统网盘将文件视作无意义的二进制块，无法理解文本语义与建立连接。`mylog` 专注于纯粹的 **Markdown** 文本，实现轻量、高性能的个人与团队知识存储底座。

* **第一阶段（MVP 网盘基座）**：
  * **核心功能**：Markdown 上传即预览、确认入库、网盘列表浏览、沉浸式高质感阅读。
  * **克制设计**：明确**不支持在线编辑修改**等易出错逻辑，专注纯粹的上传、存储与消费。
  * **安全体系**：基于 **Snowflake RBAC / DAC 混合模型** 的多用户与权限隔离体系。
* **第二阶段（向量关联与语义网络）**：
  * 引入嵌入式向量数据库（如 Orama），文件上传自动切片生成 Embedding，自动计算文章间关联，打破孤岛。
* **第三阶段（接入 AI 与专属 Agent）**：
  * 基于向量语义网络，通过 HTMX Server-Sent Events (SSE) 接入大模型，实现流式知识问答与知识沉淀。

---

## 二、第一阶段技术栈选型（已完全确立）

* **边缘运行时**：**Cloudflare Workers**（全球分布式边缘无状态计算）
* **服务层框架**：**Hono**（全栈 SSR 直出，基于 Hono JSX，零客户端运行时打包）
* **客户端交互**：**HTMX**（纯服务端驱动，~14KB，负责上传预览、无刷新入库、列表局部更新与搜索）
* **对象存储**：**Cloudflare R2**（单一共享存储桶，存放原始 `.md` 文件）
* **关系型数据库**：**Cloudflare D1**（原生 Serverless SQLite，维护用户、RBAC 授权与文件元数据）
* **安全认证**：Web Crypto API（PBKDF2-SHA256 哈希），HttpOnly 安全 Cookie 会话机制

---

## 三、权限体系设计（参考 Snowflake RBAC / DAC 模型）

用户不直连权限，特权（Privilege）授予角色（Role），角色授予用户（User）。文件所有权（OWNERSHIP）归属于角色。

### 1. 核心概念映射
* **Securable Objects（可安全对象）**：Markdown 文件（`FILE`）或 命名空间（`NAMESPACE`）。
* **Privileges（特权）**：`OWNERSHIP`（控制特权）、`READ`（阅读/预览）、`WRITE`（上传覆盖）、`DELETE`（删除）。
* **Roles（角色）**：系统预置角色（`ACCOUNTADMIN`, `PUBLIC`）或 用户私有角色。
* **Role Hierarchy（角色继承）**：父角色向上继承子角色的所有权限。

### 2. D1 核心数据表结构

```sql
-- 1. 用户表
CREATE TABLE users (
  id TEXT PRIMARY KEY,               -- usr_xxx
  username TEXT UNIQUE NOT NULL,     -- 登录用户名
  password_hash TEXT NOT NULL,       -- Web Crypto PBKDF2 加密
  is_active INTEGER DEFAULT 1,       -- 1: 启用, 0: 禁用
  created_at INTEGER NOT NULL
);

-- 2. 角色表 (权限的唯一载体)
CREATE TABLE roles (
  id TEXT PRIMARY KEY,               -- rol_xxx
  name TEXT UNIQUE NOT NULL,         -- ACCOUNTADMIN, PUBLIC, 或用户私有角色
  description TEXT,
  is_system INTEGER DEFAULT 0,       -- 1: 系统内置保护角色
  created_at INTEGER NOT NULL
);

-- 3. 用户-角色映射 (GRANT ROLE <role> TO USER <user>)
CREATE TABLE user_roles (
  user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role_id TEXT NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
  granted_by TEXT NOT NULL,
  granted_at INTEGER NOT NULL,
  PRIMARY KEY (user_id, role_id)
);

-- 4. 角色继承 (GRANT ROLE <child> TO ROLE <parent>)
CREATE TABLE role_inheritance (
  parent_role_id TEXT NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
  child_role_id TEXT NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
  created_at INTEGER NOT NULL,
  PRIMARY KEY (parent_role_id, child_role_id)
);

-- 5. 可安全对象基表
CREATE TABLE securable_objects (
  id TEXT PRIMARY KEY,               -- obj_xxx / file_xxx
  object_type TEXT NOT NULL,         -- 'FILE' 或 'NAMESPACE'
  owner_role_id TEXT NOT NULL REFERENCES roles(id), -- 所有权归属角色
  created_at INTEGER NOT NULL
);

-- 6. Markdown 文件元数据表 (与 securable_objects.id 1对1)
CREATE TABLE files (
  id TEXT PRIMARY KEY REFERENCES securable_objects(id) ON DELETE CASCADE,
  name TEXT NOT NULL,                -- 原始文件名
  title TEXT,                        -- 提取出的文章主标题
  size INTEGER NOT NULL,             -- 字节大小
  r2_key TEXT UNIQUE NOT NULL,       -- R2 存储路径: files/{owner_role_id}/{id}.md
  sha256 TEXT NOT NULL,              -- 内容哈希
  is_public INTEGER DEFAULT 0,       -- 1: 公开分享给所有访客 (PUBLIC 角色免登可读)
  created_at INTEGER NOT NULL,
  updated_at INTEGER NOT NULL
);

-- 7. 授权特权表 (GRANT <privilege> ON <object> TO ROLE <role>)
CREATE TABLE grants (
  id TEXT PRIMARY KEY,               -- grt_xxx
  role_id TEXT NOT NULL REFERENCES roles(id) ON DELETE CASCADE,
  object_id TEXT NOT NULL REFERENCES securable_objects(id) ON DELETE CASCADE,
  privilege TEXT NOT NULL,           -- 'READ' | 'WRITE' | 'DELETE' | 'OWNERSHIP'
  granted_by TEXT NOT NULL REFERENCES users(id),
  granted_at INTEGER NOT NULL
);

-- 8. 会话表
CREATE TABLE sessions (
  id TEXT PRIMARY KEY,               -- 安全随机 Token
  user_id TEXT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  current_role_id TEXT REFERENCES roles(id),
  expires_at INTEGER NOT NULL
);
```

---

## 四、HTMX 极速“上传 ➔ 预览 ➔ 确认入库”工作流规范

1. **零二次上传的暂存机制**：
   * 用户在前端拖入或选取 `.md` 文件。
   * HTMX 触发 `hx-post="/files/preview"`（`multipart/form-data`）。
   * Hono 服务端在内存中利用 `marked` 极速完成 HTML 解析并格式化代码高亮。
   * 服务端生成临时签名 Token（携带文件哈希/加密暂存内容），连同漂亮的排版 HTML 一同返回。
2. **审查与确认**：
   * 前端直接展示真实的渲染效果，包含字数、标题、代码块外观。
   * 用户点击**【确认入库】**，HTMX 提交 `upload_token` 到 `/files/confirm`。
   * 服务端将文件正式写入 Cloudflare R2，并在 D1 的 `securable_objects`、`files`、`grants` 中写入初始元数据与 OWNERSHIP 授权。
   * 网盘列表通过 HTMX `hx-swap="afterbegin"` 局部插入新记录，零页面整页刷新。
   * 若点击**【取消】**，直接清空 DOM 预览区域，无垃圾产生。

---

## 五、网盘界面交互与排版设计

1. **左侧网盘文件栏**：
   * 支持按标题/文件名实时模糊搜索（HTMX `hx-get="/files" hx-trigger="keyup changed delay:300ms"`）。
   * 列表卡片展示：文件名、标题、更新时间、文件大小、公开/私有徽章。
2. **右侧沉浸式阅读器**：
   * 点击左侧文件，HTMX 拉取并直接将解析好的 Markdown 灌入阅读区。
   * 优雅代码高亮、目录大纲导航、下载原始 `.md` 文件按钮。
   * 顶部提供“分享设置”弹窗（允许一键开启公开链接，将 `READ` 特权授予 `PUBLIC` 角色）。
3. **移动端响应式布局**：
   * 手机端下，左侧列表与右侧阅读区自动切换（点击文件进入全屏阅读，顶部带有“返回列表”按钮）。
